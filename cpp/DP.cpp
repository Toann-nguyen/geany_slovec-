#include <bits/stdc++.h>
#include <vector>
using namespace std;

#define ll long long
#define MOD 1000000007
#define MAX 1e6
#define pb push_back
#define mp make_pair
#define fi first
#define se second
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()

void solve() {
  
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
  int sum , k;
  cin>> sum >> k;
  vector<int>dp(sum);
  for(int i ; i < sum; i ++){
    cin>>dp[i];
  }
  for(int sumtotal  = 0 ; sumtotal< sum ; sumtotal++){
    for(int i = 0 ; i < sum  ; i ++){
        
    }
  }
  return 0;
}

